const removeDisableKey = () => {
 const eventTarget = document.querySelectorAll("")
  eventTarget.addEventListener("keyup", event => {
  if event.keycode === Integer
    .classList.remove('.disabled')
  };
  // do something
}


export { removeDisableKey }

