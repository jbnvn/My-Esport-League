<div>
  <%= render 'shared/dashboard_tabs' %>
</div>

<div>
  <%= image_tag "lolmap", class:"map" %>
  <%= image_tag "buttonplayer", class: 'player-input-btns', id:"top"%>
  <%= image_tag "buttonplayer", class: 'player-input-btns', id:"jungle"%>
  <%= image_tag "buttonplayer", class: 'player-input-btns', id:"mid"%>
  <%= image_tag "buttonplayer", class: 'player-input-btns', id:"adc"%>
  <%= image_tag "buttonplayer", class: 'player-input-btns', id:"support"%>
  <%= image_tag "buttonplayer", class: 'player-input-btns', id:"all"%>

</div>
<div class="full-width">
  <div class="budget">
    <div id="rest">
      <h4> Rest of budget </h4>
      <p> 300 000 PI </p> <!-- à animer -->
    </div>

    <div>
      <input type="submit" value="Create Bid" form='new_bid' disabled>
    </div>

    <div id="ongoing">
      <h4> Round bet </h4>
      <p> 40 000 PI </p>
    </div>
  </div>
</div>

<div>
  <%= render 'shared/modal_player_mercato' %>
</div>

<div class="card-formplayer">
  <%= simple_form_for [@team, @bid] do |f| %>
    <% Player::roles.keys.each do |role| %>
      <%= render 'player_role', f: f, role: role %>
    <% end %>
  <% end %>
</div>
